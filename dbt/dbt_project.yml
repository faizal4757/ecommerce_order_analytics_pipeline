name: ecommerce_dbt
version: '1.0.0'

profile: ecommerce_dbt

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

models:
  ecommerce_dbt:
    staging:
      +materialized: view
    silver:
      +materialized: table
    gold:
      +materialized: table